"""Qualitative figure: the actual recovered prompts per method/dataset, for
eyeballing legibility (does SALVE name the trait where others don't).

Corresponds to (messy source):
  experiments/sl_optimizer_comparison/plotting/plot_recovered_prompts.py

What it will do
  Pull best_text per method/dataset from the sweep JSONs and lay them out as a
  readable panel (text + behavior/legibility tags).
  Output -> plotting/figures/recovered_prompts.png.

DROP from the messy version: plotting/plot_dev_sweep.py (dev diagnostic).
"""

# TODO(stub): implement. See sl_optimizer_comparison/plotting/plot_recovered_prompts.py.
raise NotImplementedError("stub — see docstring + plot_recovered_prompts.py")
