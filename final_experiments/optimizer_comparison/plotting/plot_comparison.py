"""Main comparison figure: per-method recovery across the datasets (NLL +
behavior + legibility), the headline figure of the experiment.

Corresponds to (messy source):
  experiments/sl_optimizer_comparison/plotting/plot_comparison.py

What it will do
  Read the sweep result JSONs (per dataset, per method), draw the method
  comparison — SALVE vs baselines — with the floor / true-pi references.
  Output -> plotting/figures/method_comparison.png (via OUT_DIR = __file__.parent).
"""

# TODO(stub): implement. See sl_optimizer_comparison/plotting/plot_comparison.py.
raise NotImplementedError("stub — see docstring + plot_comparison.py")
